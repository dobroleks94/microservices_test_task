spring:
  application:
    name: service-a
  cloud:
    consul:
#      host: localhost
      host: consul
      port: 8500
      discovery:
        instance-id: ${spring.application.name}:${random.int}
server:
  port: 8020
